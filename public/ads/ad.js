/* Sacrificial ad-blocker bait. This file lives at /ads/ad.js on purpose: filter
   lists (EasyList) block that path, so if a blocker is active this script never
   runs and the flag stays unset. It is NOT a real ad and does nothing else. */
window.__abmBaitLoaded = true;
